# Retrack This brand assets

**Type:** Inter (sitewide)  
**Mark:** Exact Figma PNGs only — **do not recreate as SVG**. Resize only.

| Mode | Source | Public |
|------|--------|--------|
| Light | `source/logo-icon-light.png` | `public/brand/retrackthis-icon-light-*.png` |
| Dark | `source/logo-icon-dark.png` | `public/brand/retrackthis-icon-dark-*.png` |

Site header + loader swap via `dark:` classes.  
Favicon follows in-app theme (and `prefers-color-scheme` in metadata).  
Email / Google OAuth / Workspace use the **light** mark on white.
